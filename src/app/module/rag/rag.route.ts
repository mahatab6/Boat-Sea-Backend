import { Router } from 'express';
import { RagController } from './rag.controller';

const router = Router();

// Define your routes here
// router.post('/query', RagController.handleRagQuery);

export const RagRoutes = router;
